# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
.trae/
.reasonix/
*.swp

# OS
.DS_Store
Thumbs.db
# macOS AppleDouble 元数据（macOS→Windows 拷贝残留，2026-08-25 清理后防再跟踪）
._*

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
pytest_tmp_*/
.tmp-pytest/
# 全量测试 basetemp 残留（2026-08-26 误入跟踪，移除后防再跟踪）
.pytest_tmp_full/

# orchd 自托管（§8.4）：工作区文档与共享上下文跟踪，运行时状态忽略
# 工作区文档（task-12-workspace-docs：IDEAS/IDEAS-archive/SKILL 归置 .orchd/；
# task-skill-hub-refactor：规则目录 rules/ 归置 .orchd/rules/）
# ROADMAP.md 唯一源 = 宿主项目根（非 .orchd/ 内），纳入 git（见
# orchd/ledger.py::resolve_roadmap_path）；.orchd/ROADMAP.md 为旧布局残留，
# 引擎忽略它（resolve_roadmap_path 不读 .orchd/ 内副本）。
.orchd/*
!.orchd/_master.json
!.orchd/shared/
!.orchd/IDEAS.md
!.orchd/IDEAS-archive.md
!.orchd/SKILL.md
!.orchd/__main__.py
!.orchd/rules/
.orchd-v0-archive/

# agent 工作产物（摄入草稿 / 变更笔记 / 工具配置）
_tmp_*
.workbuddy/
reasonix.toml

# session 运行时凭据/锁（宿主接入层写入的本地会话 token 与锁文件，不入库）
.session_token.txt
.session-*.lock
.session.lock
.session.gate.lock

# 引擎运行时锁（task-audit-lock-residue-reclaim）：运行时状态，一律不入库。
# 实测教训：.session-task-p3-roadmap-ideas-map.lock 曾于 504e39c 被误提交入 git，
# 之后虽补了 .session-*.lock 规则，但 gitignore 对已跟踪文件无效；该文件被清理后
# 留下「已删除未提交」的脏状态，持续以 E017 dirty_workspace 阻塞 amend，且可复发。
# 注意：新增规则只能防未跟踪文件；已误提交的须用 git rm --cached 显式移除。
/.intake.lock
/.lock

# pytest 临时目录（误入跟踪，2026-08-05 恢复时清理）
.pytest-tmp/
# pytest --basetemp 误入跟踪（2026-08-28 done 命令误提交，清理后加入防再犯）
.pytest-basetmp/

# 构建/部署产物
orchd-dashboard/
.trae-html-share-packages/

# 2026-08-08 仓库事故现场备份（对象/refs 丢失，保留供追溯）
.git.damaged-20260808/

# orchd-core 自动同步运行日志（scripts/sync_orchd_core.sh 生成）
.orchd-core-sync.log

# 运行卫生防复发（task-runtime-hygiene-and-workspace-cleanup，2026-09-11）
# 清理任务识别的过程产物/工具目录；新增规则只防未跟踪文件再入，
# 已误提交的须 git rm --cached 显式移除。
.ruff_cache/
.codeartsdoer/
.merkle-snapshot.json
.sandbox-test-dir/
.orchd-runtime-backup-*/
_e020_tests_extract.txt
$null

# 大版本审查报告（review/：2026-09-10 全维度审查，总览 + 4 篇分维度 + 并行 agent 报告）
# 未跟踪的审查过程产物，暂不入库；文件保留在本地，需留档时再复制入 docs/
/review/

# 引擎运行时 hook 产物（task-done-cwd-hook-hygiene）：task-decl-hooks-autoset 将
# core.hooksPath 指向 .githooks 后，claim 期安装的 L3 pre-commit hook 落进被跟踪的
# .githooks/ 目录；该运行时产物须忽略，避免活跃 claim 期 git status 常显未跟踪。
.githooks/pre-commit
